# BENCHMARK CASE: V0.3-B (Code-Site Inspection)

## Target Competency
Verify targeted symbol and method inspection in `are/experience.py`.

## User Prompt
"Bagaimana cara kerja AnomalyDetector di are/experience.py dan bagaimana cara mengujinya?"

## Expected Behavior
1. Subagent `AREAnalystSubagent` or `TestRunnerSubagent` triggers targeted inspection of `are/experience.py` and `tests/are/test_experience.py`.
2. Hermes explains HMM regime shifts, spread hostility function, and quality gate criteria based on actual code evidence.
3. Automatically executes or reports live test results without instructing manual user execution.

## Assertions
- MUST cite `AnomalyDetector` details from `are/experience.py`.
- MUST include live test execution results or status (`LULUS` / `SUCCESS` / `16 passed`).
- MUST NOT tell user to execute PyTest manually.
