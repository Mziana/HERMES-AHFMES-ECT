# B13-ENGINEERING-001

## Scenario
An optimization reduces average latency in a local benchmark but increases memory consumption substantially and worsens the 99th percentile under concurrent load. No production workload profile has been supplied.

## Task
Evaluate the optimization and identify the evidence needed for a sound engineering decision.

## Constraints
Do not select the optimization based on a single average-latency number.